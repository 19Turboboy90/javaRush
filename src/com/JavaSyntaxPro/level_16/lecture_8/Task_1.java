package com.JavaSyntaxPro.level_16.lecture_8;

public class Task_1 {
    /*
    1. Если внутри блока try-with-resources возникнет ошибка, то:
    -У объекта автоматически вызовется метод close();

    2. Оператор try-with-resources:
    -может содержать секции catch и finally

    3. Из объекта InputStream нельзя:
    -записать один байт

    4. Из объекта Reader нельзя:
    -прочитать один байт

    5. Метод write(int) объекта OutputStream записывает в поток
    -один байт информации

    6. Метод write(int) объекта Writer записывает в поток
    -два байта информации

    7. Какой из примеров не скомпилируется?
    -InputStream stream = System.out

    8. Как правильно создать объект типа Path для пути заданного переменной String str?
    -Path path = Path.of(str);

    9. Что нельзя сделать с помощью метода класса Files?
    -Скопировать директорию с файлами

    10. С помощью какого метода класса Files можно быстро скопировать данные из файла в поток вывода?
    -void copy(Path, OutputStream);

     */
}
